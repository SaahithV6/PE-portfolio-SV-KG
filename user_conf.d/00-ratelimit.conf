# Rate-limit key: only POST requests are limited (1/min per IP).
map $request_method $limit_timeline {
    default "";
    POST $binary_remote_addr;
}

limit_req_zone $limit_timeline zone=timeline:10m rate=1r/m;
